# Parkids

Parkids is a kid-friendly golf score tracker for young golfers and parents. It supports 9 or 18 hole rounds, fast + / - scoring, total score tracking, round history, and progress summaries.

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the development server:
   ```bash
   npm run dev
   ```
3. Open the local URL shown by Vite.

## Project Structure

- `src/App.tsx` — main score tracker UI
- `src/main.tsx` — app entry point
- `src/styles.css` — playful app styling

## Next Steps

- Add persistence for rounds using `localStorage`
- Improve progress tracking with charts
- Add user-friendly onboarding and game goals
